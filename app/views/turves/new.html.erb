<div class="container">
  <div class="col">
    <h2><%= "#{@gang.name} gains new turf:" %></h2>
    <%= simple_form_for [@gang, @turf] do |f| %>
      <%= f.association :territory, collection: @territories, include_blank: false, include_hidden: false %>
      <%= f.submit class: 'btn btn-danger' %>
    <% end %>
  </div>
</div>
